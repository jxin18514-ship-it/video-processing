# 系统 C v2.2 视频处理长期规则

最后更新：2026-05-15

## 新窗口启动口令

`读取 01_规则文档/系统C长期规则.md，按系统 C v2.2 处理视频。`

## 固定输入输出

- 默认输入目录：`G:\视频`
- 默认输出根目录：`G:\batch_output`
- 核心管道：`G:\5.15最终版\01_脚本\batch_system_c_cut_v2.py`
- 管道模块：`G:\5.15最终版\01_脚本\wanxiang_recheck_asr_v2_pipeline.py`
- 词库目录：`G:\5.15最终版\02_词库\bad_words.txt`（193词）
- 模型目录：`G:\5.15最终版\03_模型\`
  - Qwen3-ASR-1.7B：`qwen3-asr-1.7b\`
  - Qwen3-Forced-Aligner：`qwen3-forced-aligner-0.6b\`
  - SenseVoice-Small：`models\iic\SenseVoiceSmall`

## 管道架构（9阶段）

1. 音频提取 → 2. Qwen3-ASR 转录 → 3. 违禁词检测 → 4. 去重审核
→ 5. 坏帧扫描 → 6. SenseVoice 复核 → 7. 幻觉扫描 → 8. CLEAN 审核 → 9. 视频输出

- `SKIP_SENSEVOICE=True`：跳过阶段6-7（SenseVoice复核），只用Qwen3结果
- `force=False`：跳过阶段2（ASR），使用已有CSV续跑
- `force=True`：完整重跑

## 必须遵守的规则

### 启动前：大批处理启动前只读检查规则

凡是启动以下任务前，必须先执行启动前只读检查：

- 视频批处理
- Qwen3-ASR 转录
- GPU 模型任务
- 长时间 / 多视频任务
- monitor / watchdog / supervisor
- 会生成大量输出文件的任务

以下场景不需要完整启动前检查：普通查日志、读 CSV、查看结果文件、检查单个路径。

#### 检查项 1：残留进程

检查是否存在以下残留进程（Windows: `tasklist` / PowerShell `Get-Process`）：

- python.exe / pythonw.exe（特别是 `batch_system_c_cut_v2.py`）
- ffmpeg.exe / ffprobe.exe
- monitor / watchdog / supervisor 进程

如果发现残留，必须确认其状态，必要时 kill 后再启动新批次。

#### 检查项 2：GPU 状态

```
nvidia-smi
```

- 确认 GPU 驱动正常响应（不报错、不超时）。
- 确认显存没有异常占用（例如旧进程残留的 CUDA context）。
- 如果 nvidia-smi 卡死或无响应 → 可能需要 Windows 重启重置 WDDM 驱动。

#### 检查项 3：主脚本是否为修复版

打开 `G:\5.15最终版\01_脚本\batch_system_c_cut_v2.py`，只读确认以下 4 项：

1. `cleanup_models()` 只执行 `torch.cuda.empty_cache()` + `gc.collect()`，不操作 `_MODEL_LEAK`。
2. 存在 `get_or_load_qwen3_model()` 函数。
3. `transcribe_qwen3()` 内部使用 `get_or_load_qwen3_model()` 获取模型。
4. `verify_boundaries()` 内部使用 `get_or_load_qwen3_model()` 获取模型。

如果任一项不满足 → 禁止启动，先修复脚本。

#### 检查项 4：py_compile

```bash
python -m py_compile "G:\5.15最终版\01_脚本\batch_system_c_cut_v2.py"
```

编译失败 → 禁止启动。

#### 检查项 5：输出目录安全

- 新批次输出目录不会覆盖已有批次的输出结果。
- 最终 MP4 备份目录已规划。
- 不允许自动 `rmdir` / 删除目录。
- 不允许删除、移动、覆盖已有结果文件。

预估磁盘空间：输入总大小 × 2.5 作为输出估算。确保目标磁盘有足够空间。

#### 执行要求

- 启动前检查必须只读执行。
- 检查全部 5 项通过前，**禁止**启动批处理。
- 禁止启动 monitor / watchdog / supervisor。
- 禁止修改代码。
- 禁止删除、移动、覆盖任何文件。

#### 规则来源

这条规则来源于以下历史问题：旧进程残留导致 GPU 驱动崩溃、GPU/CUDA 状态异常导致 SIGSEGV、模型重复加载导致 OOM、watchdog 误删结果目录。

### 处理中
- **不切长视频**：管道已支持186分钟视频，切分反而容易出问题。
- **ASR完成后立即释放GPU**：已在管道内置（2026-05-15修复），无需手动操作。
- **不依赖缓冲输出判断进度**：检查实际文件大小和修改时间。
- 批处理用 `subprocess.Popen` + `pythonw.exe` 启动，禁止用 bash 后台。
- 所有 subprocess 调用带 `CREATE_NO_WINDOW`。

### 崩溃恢复
- 如果 ASR 已完成（`02_asr\*_main_asr_qwen3_full_*.csv` 存在），用 `force=False` 续跑，跳过ASR。
- 如果最终 MP4 损坏（moov atom missing），从 `06_output_video\*_segments\concat_output.ts` 重建：
  ```
  ffmpeg -i concat_output.ts -c copy -movflags +faststart output.mp4
  ```
- 管道已内置 moov atom 验证+自动重试（2026-05-15修复），此问题概率已大幅降低。

### GPU 清理
管道已在 ASR 后自动释放 GPU（2026-05-15修复）。连续处理多视频时，视频间额外执行：
```python
for m in _MODEL_LEAK:
    try: m.model.to("cpu")
    except: pass
_MODEL_LEAK.clear()
torch.cuda.empty_cache()
gc.collect()
```

## 单视频处理模板

```python
# -*- coding: utf-8 -*-
import sys, os
os.environ["SKIP_SENSEVOICE"] = "True"
from pathlib import Path
import torch, gc, datetime

sys.path.insert(0, str(Path(r"G:\5.15最终版\01_脚本").resolve()))

from batch_system_c_cut_v2 import (
    set_env, load_pipeline, process_one,
    _MODEL_LEAK, WORDLIST_DIR,
)

set_env()
pipe = load_pipeline()

BATCH_DIR = Path(r"G:\batch_output\你的批次名")
VIDEO = Path(r"G:\视频\你的视频.ts")

result = process_one(pipe, BATCH_DIR, VIDEO, force=True)

# GPU cleanup
for m in _MODEL_LEAK:
    try: m.model.to("cpu")
    except: pass
_MODEL_LEAK.clear()
torch.cuda.empty_cache()
gc.collect()
```

## 文件命名规范

- 批次目录：`batch_v2_<描述>_<日期>`
- 单视频输出：`<编号>_SYSTEM_C_CUT_V2_01\`
- 最终MP4：`<编号>_cut_final_CLEAN_SYSTEM_C_CUT_V2_01.mp4`
- 命中文档：`<编号>_cut_filter_CLEAN_SYSTEM_C_CUT_V2_01.txt`

## 后续新增规则

以后用户提出新的长期偏好或固定流程，就追加到本文件。
临时状态、某个视频的处理进度、某次错误日志，不写进这里。
